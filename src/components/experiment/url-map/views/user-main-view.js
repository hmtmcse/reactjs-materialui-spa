import React, { Component } from 'react';
import {BrowserRouter as Router, Link} from 'react-router-dom';

export default class UserMainView extends Component {
    render() {
        return (<h1>User Main View</h1>);
    }
}