import React, { Component } from 'react';
import {BrowserRouter as Router, Link} from 'react-router-dom';

export default class LoginView extends Component {
    render() {
        return (<h1>Only Login View</h1>);
    }
}
